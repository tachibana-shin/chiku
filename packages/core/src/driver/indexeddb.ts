import { del, delMany, get, getMany, set, setMany, update } from "idb-keyval"

export {
  get as get$,
  set as set$,
  del as del$,
  getMany as getMany$,
  setMany as setMany$,
  delMany as delMany$,
  update as update$
}
